plugins {
    id 'java'
    id 'org.jetbrains.kotlin.jvm'
}

sourceSets {
    main {
        resources {
//            srcDir "${project(':drive').projectDir}/src/main/resources"
        }
    }
}
dependencies {
    compile project(path: ':filesync')
    testCompile project(configuration: 'driveTestOutput', path: ':filesync')
    testCompile 'junit:junit:4.12'
}
